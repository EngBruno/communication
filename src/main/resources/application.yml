spring:
  application:
    name: user-pca
  jpa:
    database: postgresql
    show-sql: true
    properties: 
      hibernate: 
        temp.use_jdbc_metadata_defaults: false
  datasource:
    url: jdbc:postgresql://localhost:5432/communication
    username: postgres
    password: postgres
    hikari:
      maximum-pool-size: 1
  
  jackson:
    default-property-inclusion: NON_NULL

 